import type { RumConfiguration } from '@datadog/browser-rum-core';
import type { RumProfilerTrace } from '../types';
export type SendProfileFunction = (trace: RumProfilerTrace, configuration: RumConfiguration, sessionId: string | undefined) => Promise<unknown>;
export declare const transport: {
    sendProfile: SendProfileFunction;
};
